# Ultra-simple standalone backend - NO dependencies needed
FROM node:18-alpine

WORKDIR /app

# Copy just the standalone JavaScript file
COPY simple-backend.js ./app.js

# Expose port
EXPOSE 4000

# Start the application
CMD ["node", "app.js"]